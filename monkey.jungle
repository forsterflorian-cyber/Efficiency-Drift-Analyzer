project.manifest = manifest.xml

# High-memory-only target stack. The legacy low-memory branch remains in source
# for now, but is excluded globally from every build.
base.excludeAnnotations = low_mem
